<Window x:Class="NolowaFrontend.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NolowaFrontend.Views"
        xmlns:uc="clr-namespace:NolowaFrontend.Views.UserControls"
        xmlns:diag="clr-namespace:System.Diagnostics;assembly=WindowsBase"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d" WindowStyle="None" 
        MouseDown="Window_MouseDown" Closed="Window_Closed"        
        Title="MainView" d:Height="600" d:Width="400"
        Height="600" Width="400">
    <!--, diag:PresentationTraceSources.TraceLevel=High-->
    <Window.Resources>
        <sys:Double x:Key="diameterOfTweet">50</sys:Double>
    </Window.Resources>

    <Grid>      
        <DockPanel>
            <Grid DockPanel.Dock="Top" Height="50" Background="Red">
                
            </Grid>

            <Grid DockPanel.Dock="Bottom" Height="70" Background="Aqua">
                <StackPanel Orientation="Horizontal">
                    <Button Width="80"/>
                    <Button Width="80"/>
                    <Button Width="80"/>
                    <Button Width="80"/>
                    <Button Width="80"/>
                </StackPanel>
            </Grid>

            <Grid>
                <ListBox ItemsSource="{Binding Posts}"
                         ScrollViewer.VerticalScrollBarVisibility="Disabled"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <uc:PostView/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </DockPanel>

        <Button x:Name="TweetButton" Width="{StaticResource diameterOfTweet}" Height="{StaticResource diameterOfTweet}" Margin="0,0,20,80"
                VerticalAlignment="Bottom" HorizontalAlignment="Right" Command="{Binding TweetCommand}">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Grid >
                                    <Ellipse Width="{StaticResource diameterOfTweet}" Height="{StaticResource diameterOfTweet}" Fill="Aquamarine"/>
                                    <Image Source="/Resources/Icons/pencil.png" Margin="5"/>
                                    <ContentControl/>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>

        <ContentControl Content="{Binding TweetView}"/>
    </Grid>
</Window>